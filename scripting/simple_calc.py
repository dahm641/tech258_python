# module is a single file importing in.
# package is multiple modules.
# library is a big package / large packages - less defined. Catch all term.

from math_operators import *

input1 = int(input("first number you want to add: "))
input2 = int(input("second number to add: "))

print(add(input1, input2))
